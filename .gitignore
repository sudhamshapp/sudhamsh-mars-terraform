# Ignore Terraform state files and backups
terraform.tfstate
terraform.tfstate.backup

# Ignore any other Terraform state files with extensions (e.g., .tfstate.<timestamp>)
*.tfstate.*
 
# Ignore the .terraform directory (contains provider and module caches)
.terraform/

# Ignore Terraform lock file (optional, can be kept or removed depending on your workflow)
.terraform.lock.hcl
